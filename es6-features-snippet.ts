//Class syntax
class MyClass {
   name;

   constructor(name) {
      this.name = name;
   }
}

//Lambda expression
var func = (num) => num * num;

//String formatting
var dims = { height: 10, width: 20 };

var msg = `Area = ${dims.height * dims.width}`;